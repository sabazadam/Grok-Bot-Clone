{
  "name": "botbox",
  "version": "0.1.0",
  "private": true,
  "description": "Self-hosted team of always-on AI agents, each with its own computer",
  "license": "MIT",
  "workspaces": [
    "server",
    "web"
  ],
  "scripts": {
    "dev": "concurrently -n server,web -c blue,magenta \"npm --workspace server run dev\" \"npm --workspace web run dev\"",
    "build": "npm --workspace web run build && npm --workspace server run build",
    "start": "npm --workspace server run start",
    "build:computer": "docker build -t botbox-computer:latest computer/"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
